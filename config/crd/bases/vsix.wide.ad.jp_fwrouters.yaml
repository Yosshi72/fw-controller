---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: fwrouters.vsix.wide.ad.jp
spec:
  group: vsix.wide.ad.jp
  names:
    kind: FwRouter
    listKind: FwRouterList
    plural: fwrouters
    singular: fwrouter
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: FwRouter is the Schema for the fwrouters API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: FwRouterSpec defines the desired state of FwRouter
            properties:
              extensions:
                additionalProperties:
                  type: string
                type: object
              zones:
                additionalProperties:
                  properties:
                    allowPrefixNames:
                      items:
                        type: string
                      type: array
                    interfaces:
                      items:
                        type: string
                      type: array
                    zonePolicy:
                      type: string
                  required:
                  - zonePolicy
                  type: object
                type: object
            required:
            - zones
            type: object
          status:
            description: FwRouterStatus defines the observed state of FwRouter
            properties:
              extensions:
                additionalProperties:
                  type: string
                type: object
              zones:
                additionalProperties:
                  properties:
                    allowPrefixNames:
                      items:
                        type: string
                      type: array
                    created:
                      type: boolean
                    interfaces:
                      items:
                        type: string
                      type: array
                    zonePolicy:
                      type: string
                  required:
                  - created
                  - zonePolicy
                  type: object
                type: object
            required:
            - zones
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
